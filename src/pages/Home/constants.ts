import { serverIp, serverPort } from '@shared/constants';

export const githubLink = 'https://github.com/Ragna13377';
export const apiSearch = `https://${serverIp}:${serverPort}/api/search`;
export const selectOptions = ['All', 'Steam', 'GOG'];
